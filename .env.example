# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SEARXNG_SECRET=replace-with-at-least-32-random-characters

# Optional. Vercel sets the public URL after deployment; set this manually when needed.
# SEARXNG_BASE_URL=https://your-project.vercel.app/
